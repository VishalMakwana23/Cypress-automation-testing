# cypress assertion

1. Implicit Assertion

    - should
    - and

    (it Contains the following)

        -- eq
        -- contain
        -- exist
        -- have.length
        -- have.value
        etc..

2. Explicit Assertion

    - expect - BDD
    - assert - TDD